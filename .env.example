# Copy this file to .env before running docker compose.
# Use a strong initial password on first startup. After it is written to SQLite,
# changing this value will not reset the existing password.
PROXY_GO_INITIAL_PASSWORD=change-me-now

# Email used for ACME certificate registration and renewal notices.
PROXY_GO_ACME_EMAIL=admin@example.com
